/*  applyDiscount(1000, 20) returns 1200 instead of a smaller discounted price of 800.  */

function applyDiscount(price, percent) { 
let discount = (price * percent / 100); 
return  discountedPrice = price - discount
} 
console.log(applyDiscount(1000, 20)); 
