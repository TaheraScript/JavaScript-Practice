/* Always prints 'Perfect score!' no matter what score actually is — AND score gets overwritten to 100.*/ 


let score =190; 
let newScore = 100;
score =newScore //score gets overwritten to 100(newScore)
 
if (score = 100) { 
console.log('Perfect score!'); 
} else { 
console.log('Score:', score); 
} 


