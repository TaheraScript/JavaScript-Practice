let numbers = [11,12,13,14,15,17];
let reversed = [];
for( let i=numbers.length-1 ; i>= 0 ; i--){
    console.log('index position',i,'the array is', numbers[i]);
   reversed.push(numbers[i]);
   console.log(reversed);

}