// Task 5.2 — Watch the Video Concept
// After watching the "What is an Object" video, write 2–3 sentences (as a comment in your code) explaining an object in your own words, using a real-life analogy (not a car or a person — get creative!).

// object is a function.it works like a container,where we can store more than one values&keys.the values can be String,number,boolean,object,etc.we have to follow some rules for writing an object.they are the key/property and value should be written under third bracket.key and values should be written in pairs like, key:value.and we must give comma between every pairs. we can write a function under a object.we can call all keys at a time using Object.keys & can call all values at a time using Object.values ,we can also call key&value as a pair using Object.entires.we also can separately call them.we can use reverseIt,split,concat,join,loops,etc on it.object is mutable.means we can change the key or value name in object.we can properly describe a a thing by using an object.